export default function Vault() {
  return (
    <div className="bg-black text-white min-h-screen flex flex-col">
      <main className="flex-grow flex flex-col items-center justify-center px-6">
        <h1 className="text-4xl font-bold text-green-400">Vault</h1>
        <p className="text-gray-400 mt-4 max-w-2xl text-center">
          Experience the QFSync vault — a quantum-protected hub where assets sync
          instantly across the financial grid, with transparency and stability.
        </p>
      </main>
    </div>
  )
}